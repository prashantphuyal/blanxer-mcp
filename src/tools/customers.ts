// Blanxer API does not expose a standalone customers endpoint.
// Customer data is accessed via orders (customer_full_name, customer_phone_number, etc.).
// This file is intentionally empty — customer tools are handled within order tools.
export {};
